code cleanup / refactoring in instances.c

add a PROPERTIES FOLDER to list the properties available for an entity
add a GET command, generic getter for entity properties
pass a pointer to command handlers for retrieving an error message
implement the PULL command, with libcurl
add a lua config file for each firmware
allow to associate a "product type" to a firmware
add a inotify watch on firmwares in the repository
implement all the TODOs in the source

only keep base_workspace, don't allow to change the others
